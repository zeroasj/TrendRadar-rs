[package]
name = "trendradar-embedded"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
trendradar-core = { path = "../core", optional = true }
serde.workspace = true

[features]
default = []
esp32 = []
